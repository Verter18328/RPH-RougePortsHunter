# docs/ — celowo NIE ignorujemy: plan audytu ma być w repo (współpraca, klon, backup).
#        Jeśli potrzebujesz prywatnych notatek, użyj np. docs/private/ i dodaj: docs/private/

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/

# Sekrety i lokalna konfiguracja SSH/haseł
.env
.env.*
!.env.example

# RPH — dane operacyjne audytu (plan: inventory z ~500 hostami; nie w repo)
inventory.csv

# Wyniki audytu i surowe wyjścia CLI (wrażliwe / duże)
output/
logs/
*.log
raw/
